# T008 Report

## Status

- Completed: Yes
- CodeGraph used: No. Repository index exists under `.codegraph/`, but this Codex session did not expose a callable CodeGraph CLI or `codegraph_*` MCP tool entry.
- Ponytail used: Yes. Applied the minimal header-only design and ran a ponytail-style diff review result: `Lean already. Ship.`

## Pre-check Notes

- `speckit-implement` optional pre-hook was detected but not executed:
  - Extension: `git`
  - Command: `/speckit.git.commit`
- Running on `main` is workable for this repository by setting `SPECIFY_FEATURE=001-cross-platform-raft` when invoking Spec Kit scripts.
- `.specify/scripts/bash/check-prerequisites.sh --json --require-tasks --include-tasks` passed with `SPECIFY_FEATURE=001-cross-platform-raft`.
- Checklist status under `specs/001-cross-platform-raft/checklists/` was PASS.

## Files Created Or Modified

- Modified: `cross-platform-raft/CMakeLists.txt`
- Created: `cross-platform-raft/src/raft/CMakeLists.txt`
- Created: `cross-platform-raft/src/raft/raft_types.h`
- Created: `cross-platform-raft/src/raft/raft_message.h`
- Created: `specs/001-cross-platform-raft/report/T008.md`

## Added Enums And Base Types

- `RaftRole`: `FOLLOWER`, `CANDIDATE`, `LEADER`, `LEARNER`
- `NodeState`: `RUNNING`, `STOPPED`, `FAILED`
- `LogEntryType`: `NO_OP`, `COMMAND`, `MEMBERSHIP_CHANGE`
- Reused existing common IDs:
  - `cpr::common::NodeId`
  - `cpr::common::Term`
  - `cpr::common::LogIndex`
- Added compact raft-local data holders:
  - `OpaquePayload`
  - `NodeAddress`
  - `RaftMember`

## Added Raft Data Structures

- `MembershipView`
  - Separate `voters` and `learners` collections
  - `has_active_transition`
  - `configuration_id`
- `LogEntry`
  - `index`
  - `term`
  - `type`
  - `payload`
- `HardState`
  - `current_term`
  - `voted_for`
  - `commit_index`
  - `membership_configuration_id`
- `SnapshotMetadata`
  - `last_included_index`
  - `last_included_term`
  - `membership`
- `PeerProgress`
  - `node_id`
  - `is_learner`
  - `is_active`
  - `match_index`
  - `next_index`

## Added Protocol Messages

- `RequestVoteRequest`
- `RequestVoteResponse`
- `AppendEntriesRequest`
- `AppendEntriesResponse`
- `InstallSnapshotRequest`
- `InstallSnapshotResponse`
- `RaftMessageType`
- `RaftMessagePayload` as `std::variant`
- `RaftMessage` unified wrapper with source/target node IDs

## Module Dependency Result

- Added `src/raft` as a new CMake subdirectory.
- Added `cpr_raft` as an `INTERFACE` target.
- `cpr_raft` depends only on `cpr_common`.
- No includes or links to `config`, `platform`, `server`, `metadata`, `store`, or `rpc` were introduced.
- No time, file, thread, platform API, gRPC, Protobuf, or JSON usage was introduced.

## Build And Verification

Commands executed:

```bash
cmake --preset debug-ninja-low-parallel
cmake --build --preset debug-ninja-low-parallel --parallel 2
```

Results:

- CMake configure: PASS
- Build with at most 2 parallel jobs: PASS
- Header smoke compile for `raft/raft_types.h`: PASS
- Header smoke compile for `raft/raft_message.h`: PASS
- Verified the new structs and messages can be default-constructed and fields can be accessed in temporary compile checks
- Temporary verification files were removed after validation

Notes:

- `cpr_raft` is an `INTERFACE` target, so Ninja does not emit a standalone build artifact target for it. Dependency scope was verified from `src/raft/CMakeLists.txt`.

## Parallelism Limit

- Maximum parallel build jobs used: 2
- No unrestricted `-j` or `-j$(nproc)` was used

## Scope Control

- No production code outside the allowed raft files was modified except the required root `CMakeLists.txt` wiring.
- No tests were added or modified.
- No RaftLog, RaftCore, storage, RPC, metadata, store, or server behavior was implemented ahead of schedule.

## Deviations / Blockers / Unfinished

- CodeGraph was required by the task, but no callable CodeGraph entry was exposed to this Codex session even though the repository index directory exists.
