# T015 Report

## Status

- Completed: Yes
- CodeGraph used: Yes
- Ponytail used: Yes

## Files Created Or Modified

- Created: `cross-platform-raft/tests/raft_core_test.cpp`
- Modified: `cross-platform-raft/tests/CMakeLists.txt`
- Modified: `CMakeLists.txt`
- Created: `specs/001-cross-platform-raft/report/T015.md`

## Test Target And Count

- Test target: `cpr_raft_core_tests`
- CTest prefix: `raft_core.`
- RaftCore test cases: 9
- Full CTest cases after registration: 46

Root `CMakeLists.txt` was minimally updated to include CTest and add the `cross-platform-raft` subdirectory. Without this, the configured preset built only the root placeholder target and CTest discovered zero tests.

## Tick, Role Conversion, And Election Coverage

Covered by:

- `InitializationAndLogicalTicksDriveDeterministicElections`
- `RoleAndTermTransitionsRejectInvalidMovesAtomically`

Validated:

- initial role, term, vote, and leader state
- follower election timeout before and after threshold
- candidate timeout starts a new term
- election self-vote
- leader tick does not trigger election
- learner tick does not trigger election
- candidate can become leader
- higher term returns leader/candidate to follower
- lower term transition is rejected without mutation
- learner cannot become candidate or leader

Result: PASS

## RequestVote And Heartbeat Coverage

Covered by:

- `RequestVoteHonorsTermLogVoteAndLearnerRules`
- `HigherTermVoteResponseStepsCandidateDown`
- `HeartbeatBuildAndHandlingUseAppendEntriesSemantics`

Validated:

- stale RequestVote rejection
- higher-term RequestVote term update and follower state
- log freshness rejection and grant paths
- same-term double-vote rejection
- repeated same-candidate vote consistency
- learner vote rejection
- higher-term vote response step-down
- leader heartbeat construction with empty entries
- heartbeat leader tracking and election tick reset
- stale heartbeat rejection
- higher-term heartbeat step-down
- heartbeat prev-log mismatch rejection

Result: PASS

## AppendEntries And Conflict Catch-Up Coverage

Covered by:

- `AppendEntriesHandlesDuplicatesConflictsCommitsAndTerms`
- `LeaderPeerProgressBackoffAndCatchupAreDeterministic`

Validated:

- accepting entries when previous index and term match
- conflict metadata for mismatched previous log
- duplicate entries are not appended again
- uncommitted conflicting suffix can be replaced
- committed suffix truncation is rejected
- leader commit is capped by local `lastIndex`
- stale AppendEntries rejection
- higher-term AppendEntries step-down
- peer `nextIndex` drives AppendEntries construction
- success response advances `matchIndex` and `nextIndex`
- repeated older success does not move progress backward
- `conflictIndex` and `conflictTerm` backoff
- externally driven multi-step follower catch-up
- higher-term append response steps leader down

Result: PASS

## PeerProgress, Majority Commit, And Learner Coverage

Covered by:

- `LeaderPeerProgressBackoffAndCatchupAreDeterministic`
- `MajorityCommitCountsOnlyVotersAndCurrentTermEntries`

Validated:

- leader initializes local and remote peer progress
- learner progress is marked as learner
- 3 voters need 2 matching replicas
- 5 voters need 3 matching replicas
- leader local progress counts toward quorum
- learner replication does not count toward quorum
- old-term entry does not directly advance leader commit
- current-term entry replicated to majority advances commit
- commit index does not move backward or past `lastIndex`

Result: PASS

## Apply-Ready And RaftOutput Coverage

Covered by:

- `ApplyReadyAndOutputBatchesReflectPersistenceGates`

Validated:

- empty apply-ready range is explicit
- committed range is `appliedIndex + 1 .. commitIndex`
- term/vote/commit changes produce HardState output
- unstable entries are emitted as a continuous range
- stable entries are not emitted again after `ConfirmPersistence`
- granted vote response is held in persisted messages
- successful AppendEntries response is held until log and required HardState persistence
- stale rejection response is immediately sendable
- `ConfirmPersistence()` releases blocked messages after dependencies are satisfied
- repeated output reads do not mutate term, role, or log state

Result: PASS

## Build And Test Commands

Checklist status:

```text
| Checklist       | Total | Completed | Incomplete | Status |
|-----------------|-------|-----------|------------|--------|
| requirements.md | 16    | 16        | 0          | PASS   |
```

Commands:

```bash
cmake --preset debug-ninja-low-parallel
cmake --build --preset debug-ninja-low-parallel --parallel 2
ctest --preset debug-tests --output-on-failure
ctest --preset debug-tests -R raft_core --output-on-failure
```

Results:

- configure: PASS
- build: PASS
- full CTest: PASS (`46/46`)
- RaftCore CTest filter: PASS (`9/9`)

## Parallelism Limit

- Build parallelism was limited to at most 2 jobs
- No unbounded `-j`, `-j$(nproc)`, or concurrent build execution was used

## Production Code Changes

- `src/raft/raft_core.h`: Not modified
- `src/raft/raft_core.cpp`: Not modified
- `src/raft/raft_log.h/.cpp`: Not modified
- `src/raft/raft_message.h`: Not modified
- Other production source modules: Not modified

Only CMake test/build discovery files and test/report files were changed.

## Scope Control

- Storage: Not implemented
- Runtime: Not implemented
- Transport: Not implemented
- RPC: Not implemented
- T016 or later functionality: Not implemented
- No real time, sleep, random input, threads, file access, or network access was used in RaftCore tests

## Deviations, Blockers, Or Unfinished Items

- No remaining blockers for T015
- Tests use existing public APIs only; no private-state accessor was added
- The current RaftCore still has no formal proposal API, so tests use existing in-memory AppendEntries paths to create local leader log state for simulation-only majority-commit checks
