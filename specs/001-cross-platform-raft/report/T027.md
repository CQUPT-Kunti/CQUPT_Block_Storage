# T027 Report — Protobuf and gRPC Code Generation Integration

## Status

- **Completed**: Yes
- **CodeGraph used**: Yes
- **Ponytail used**: Yes

## Files Created or Modified

- **Modified**: `cross-platform-raft/cmake/Dependencies.cmake`
- **Modified**: `cross-platform-raft/proto/CMakeLists.txt`
- **Modified**: `specs/001-cross-platform-raft/tasks.md` (checkbox only)
- **Created**: `specs/001-cross-platform-raft/report/T027.md`

## Dependency Detection

T027 reuses the T002 dependency discovery path in `cross-platform-raft/cmake/Dependencies.cmake`:

- `find_package(Protobuf ...)`
- `find_package(gRPC CONFIG QUIET)`

No new package-discovery layer, `FetchContent`, auto-download, source build, or environment mutation was added.

Tool detection is layered on top of the existing package results only:

- `protoc`: prefer imported target `protobuf::protoc`, otherwise use `Protobuf_PROTOC_EXECUTABLE`
- `grpc_cpp_plugin`: prefer imported target `gRPC::grpc_cpp_plugin`, otherwise use `find_program(grpc_cpp_plugin)`

Detection results in the current environment:

- `Protobuf`: found
- `gRPC`: found
- `protoc`: found
- `grpc_cpp_plugin`: found

Resolved tool sources:

- `protoc`: imported target `protobuf::protoc`, cache variable `Protobuf_PROTOC_EXECUTABLE=/home/yangjilei/vcpkg/installed/x64-linux/tools/protobuf/protoc`
- `grpc_cpp_plugin`: imported target `gRPC::grpc_cpp_plugin`, imported debug path `/home/yangjilei/vcpkg/installed/x64-linux/tools/grpc/grpc_cpp_plugin`

The detection exports these internal CMake state variables for proto generation:

- `CPR_PROTOC_AVAILABLE`
- `CPR_PROTOC_EXECUTABLE`
- `CPR_GRPC_CPP_PLUGIN_AVAILABLE`
- `CPR_GRPC_CPP_PLUGIN_EXECUTABLE`
- `CPR_PROTO_CODEGEN_AVAILABLE`
- `CPR_PROTO_CODEGEN_MISSING_COMPONENTS`

## Code Generation Targets

`cross-platform-raft/proto/CMakeLists.txt` now keeps two separate responsibilities:

- `cpr_proto_contracts`
  - source-only custom target listing maintained `.proto` contracts
- `cpr_proto_generated`
  - static library compiled from build-directory generated `.pb.cc` and `.grpc.pb.cc`
  - created only when protobuf + gRPC code generation is fully available

Generated output directory:

- `cross-platform-raft/build/linux/cross-platform-raft/proto/generated/`

Generated file set:

- Protobuf for all T026 proto files:
  - `common.pb.h/.cc`
  - `raft.pb.h/.cc`
  - `metadata.pb.h/.cc`
  - `store.pb.h/.cc`
- gRPC for service-bearing proto files:
  - `raft.grpc.pb.h/.cc`
  - `metadata.grpc.pb.h/.cc`
  - `store.grpc.pb.h/.cc`

## Link Relationships

`cpr_proto_generated`:

- `PUBLIC` links:
  - `protobuf::libprotobuf`
  - `gRPC::grpc++`
- `PUBLIC` include path:
  - generated build directory only
- `PRIVATE` links:
  - `cpr_project_options`
  - `cpr_project_warnings`
  - `cpr_platform_options`

No existing `common`, `config`, `platform`, `raft`, `server`, `metadata`, `store`, or RPC production source target was changed to depend on generated protobuf/gRPC code.

## Import and Regeneration Behavior

- `common.proto` is imported by the service-level proto files through `--proto_path`
- Both protobuf and gRPC generation commands depend on the full proto file set, so changing any proto re-triggers regeneration
- Generated files stay in the build tree and are not written into the source tree
- No `.pb.h`, `.pb.cc`, `.grpc.pb.h`, or `.grpc.pb.cc` files were hand-written or committed

## Degraded Behavior When Dependencies Are Incomplete

- Configuration still succeeds when protobuf/gRPC tools are incomplete
- In that case, `cpr_proto_generated` is not created
- `cpr_proto_contracts` remains available
- CMake emits a warning containing `CPR_PROTO_CODEGEN_MISSING_COMPONENTS`
- The build does not pretend generation succeeded

## Proto Changes

- **Proto files modified**: No
- Reason: current T026 proto contracts were already syntactically suitable for generation wiring; no real schema blocker needed correction

## Verification Commands

Commands used:

```bash
cmake --preset debug-ninja-low-parallel
cmake --build --preset debug-ninja-low-parallel --parallel 2 --target cpr_proto_generated
touch cross-platform-raft/proto/common.proto
cmake --build --preset debug-ninja-low-parallel --parallel 2 --target cpr_proto_generated
cmake --build --preset debug-ninja-low-parallel --parallel 2
ctest --preset debug-tests --output-on-failure
```

Results:

- `cmake --preset debug-ninja-low-parallel`: **PASS**
- `cmake --build --preset debug-ninja-low-parallel --parallel 2 --target cpr_proto_generated`: **PASS**
- `touch cross-platform-raft/proto/common.proto` + rebuild target: **PASS**
- `cmake --build --preset debug-ninja-low-parallel --parallel 2`: **FAIL**
- `ctest --preset debug-tests --output-on-failure`: **FAIL**

Verification details:

- CMake recognizes all four proto files through `cpr_proto_contracts`
- C++ protobuf generation runs for `common.proto`, `raft.proto`, `metadata.proto`, and `store.proto`
- gRPC generation runs for `raft.proto`, `metadata.proto`, and `store.proto`
- Generated files are emitted under:
  - `build/linux/cross-platform-raft/proto/generated/`
- Generated files successfully compile into `libcpr_proto_generated.a`
- No existing source module was changed to link against `cpr_proto_generated`

Regeneration check:

- Before `touch common.proto`:
  - `common.pb.cc` timestamp: `1784255429`
  - `raft.grpc.pb.cc` timestamp: `1784255429`
- After rebuild:
  - `common.pb.cc` timestamp: `1784255470`
  - `raft.grpc.pb.cc` timestamp: `1784255470`

This confirms that a proto file change re-triggers the generation rules.

Log paths:

- `tmp/test-logs/t027-cmake-configure.log`
- `tmp/test-logs/t027-build-proto-target.log`
- `tmp/test-logs/t027-build-proto-regen.log`
- `tmp/test-logs/t027-build.log`
- `tmp/test-logs/t027-ctest.log`
- `tmp/test-logs/t027-probe-targets.log`
- `tmp/test-logs/t027-cmake-configure-probe.log`

Full build failure summary:

- The generated proto target itself builds successfully
- The full build still fails later at GoogleTest discovery for existing test executables
- The failure remains the pre-existing host runtime-library mismatch:
  - `GLIBCXX_3.4.32` not found
  - `GLIBCXX_3.4.30` not found
- The failing runtime library path remains `/home/yangjilei/anaconda3/lib/libstdc++.so.6`

CTest summary:

- `ctest --preset debug-tests --output-on-failure` fails because several test executables were not built after the full build stopped during `gtest_discover_tests`
- The GLIBCXX environment issue is still present and unchanged by T027

## Parallelism Limit

- Maximum parallelism used: **2**
- No unrestricted `-j`
- No `-j$(nproc)`
- No concurrent build commands

## Scope Guard

- Transport implemented early: **No**
- RPC service implemented early: **No**
- Server startup implemented early: **No**
- Runtime / RaftCore / Storage integration implemented early: **No**
- Message conversion functions implemented early: **No**

## tasks.md Marking

- T001-T026 remained checked
- T027 checked after implementation and verification
- T028 and later tasks remain unchecked

## Deviations, Blockers, or Unfinished

- No deviation from T027 scope: only dependency detection and code-generation wiring were implemented.
- Existing unrelated worktree change observed before T027: `vcpkg.json` was already modified and was left untouched.
- Full build and `ctest` remain blocked by the pre-existing GLIBCXX / Anaconda runtime issue, but targeted protobuf/gRPC code generation completed successfully.
